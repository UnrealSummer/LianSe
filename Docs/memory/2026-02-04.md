# 2026-02-04 工作日志

## 《炼色》项目 - 第一关完成，第二关问题待解决

### 今日成就
- ✅ 解决了方块位置不一致的重大问题
- ✅ 实现了自适应网格大小
- ✅ 完成了第一关的所有核心功能
- ✅ 词条选择界面正常工作
- ✅ 修复了击败敌人后游戏继续运行的问题

### 解决的关键问题

#### 1. 方块位置不一致问题 ⭐
**现象：**
- 初始生成的方块位置正确，间隔合适
- 消除后新生成的方块位置偏移
- 方块叠在一起，间隔不够

**调试过程：**
1. 发现新生成方块的 X 坐标不对（-262.5 vs -238.0）
2. 添加日志对比 blockSize 和 spacing
3. 发现初始生成用的是 60/8，消除后用的是 64/11
4. 追踪到 Cocos Creator 的 start() 执行顺序问题

**根本原因：**
- Cocos Creator 的 start() 执行顺序不确定
- GameCore.start() 可能先于 GridSystem.start() 执行
- generateGrid() 使用了场景配置的默认值（60, 8）
- calculateAdaptiveSize() 计算出新值（64, 11）
- 导致初始生成和后续生成使用不同的参数

**最终解决方案：**
```typescript
// GridSystem.ts - generateGrid()
generateGrid(): void {
    // 每次生成前都重新计算自适应大小（确保一致）
    this.calculateAdaptiveSize();
    // ...
}
```

**关键经验：**
- ⚠️ **Cocos Creator 的 start() 不保证执行顺序！**
- ✅ **关键参数计算要在使用前立即执行，不要依赖 start()**
- ✅ **位置计算要统一使用同一个方法（getBlockPosition）**
- ✅ **setParent 会影响位置，要在 setParent 后再次 setPosition**
- ✅ **Widget 组件会强制设置位置，要在 setParent 前禁用**

#### 2. 自适应网格大小
**问题：** 8×8 格子超出屏幕，方块叠在一起

**解决方案：**
```typescript
calculateAdaptiveSize(): void {
    const spacingRatio = 0.18; // 间隔占18%
    const blockSize = availableWidth / (gridSize + (gridSize - 1) * spacingRatio);
    this.blockSize = Math.floor(Math.min(widthBlockSize, heightBlockSize));
    this.spacing = Math.floor(this.blockSize * spacingRatio);
}
```

**效果：**
- 5×5 格子 → 方块更大，间隔更大
- 8×8 格子 → 方块适中，间隔适中
- 10×10 格子 → 方块更小，间隔更小

#### 3. 击败敌人后游戏继续运行的问题
**现象：**
- 击败敌人后，词条选择界面显示
- 但是游戏还在继续消除方块
- 连锁消除继续触发，又击败了敌人

**解决方案：**
```typescript
// GameCore.ts - processMatches()
private processMatches(matches: any[][]): void {
    // 检查游戏是否还在运行
    if (!this.isGameRunning) {
        console.log(`[GameCore] 游戏已停止，跳过消除处理`);
        this.gridSystem.setProcessing(false);
        return;
    }
    // ...
}

// 掉落后检测新消除时也检查
this.scheduleOnce(() => {
    if (!this.isGameRunning) {
        console.log(`[GameCore] 游戏已停止，跳过连锁检测`);
        this.gridSystem.setProcessing(false);
        return;
    }
    // ...
}, 0.5);
```

### 调试技巧总结
1. **添加详细日志** - 显示计算值和实际值的对比
2. **对比初始和后续** - 找出参数变化的时机
3. **不盲目改参数** - 先找到根本原因再修复
4. **统一位置计算** - 使用同一个方法避免不一致
5. **清理不必要的日志** - 保持输出清爽，便于问题查找

### 第一关功能清单 ✅
- ✅ 8×8 网格生成（自适应大小）
- ✅ 方块间隔合适，不会叠在一起
- ✅ 初始不会有可消除的组合
- ✅ 玩家滑动交换方块
- ✅ 消除匹配的方块
- ✅ 上方方块向下掉落
- ✅ 顶部生成新方块
- ✅ 连锁检测和伤害计算
- ✅ 击败敌人后显示词条选择
- ✅ 点击词条按钮正常工作

### ⚠️ 第二关问题（待解决）

**现象：**
- 击败第一关敌人，选择词条后
- 第二关开始（敌人 Lv.2 HP:80 显示）
- **但是看不到格子！只有敌人血条和UI**

**已知信息：**
1. 日志显示 generateGrid 被调用
2. 自适应计算完成（blockSize=64, spacing=11）
3. gridContainer 已配置
4. clearGrid 被调用
5. **但是之后没有看到方块生成的日志**

**可能的原因：**
1. 方块生成了但不可见（透明度？层级？颜色？）
2. 方块位置不对（在屏幕外？）
3. gridContainer 的 active 状态或位置有问题
4. 方块被其他UI遮挡
5. for 循环没有执行（但这不太可能）

**已添加的调试日志：**
```typescript
// GridSystem.ts - generateGrid()
console.log(`[GridSystem] 容器状态: active=${this.gridContainer.active}, position=(${this.gridContainer.position.x}, ${this.gridContainer.position.y})`);
// ...
console.log(`[GridSystem] 生成完成，容器子节点数: ${this.gridContainer.children.length}`);
```

**下一步调试计划：**
1. 运行游戏到第二关
2. 查看日志中的容器状态和子节点数
3. 如果子节点数是 64，说明方块生成了，问题是可见性
4. 如果子节点数是 0，说明方块没有生成，需要检查 for 循环
5. 检查 gridContainer 的 active 和 position
6. 检查方块的 active、opacity、color 等属性

**可能的修复方案：**
- 如果是可见性问题：检查方块的 opacity、color、layer
- 如果是位置问题：检查 gridContainer 的 position
- 如果是层级问题：检查 zIndex 或渲染顺序
- 如果是遮挡问题：调整 UI 层级

---

## 工作风格
- sansheng 说我"处理问题的速度越来越快了" 👍
- 关键是：先找到根本原因，不盲目改参数
- 详细的日志对比是快速定位问题的关键
- 清理不必要的日志，保持输出清爽

---

## 明天的任务
1. **优先：解决第二关格子不显示的问题**
   - 运行游戏到第二关
   - 查看容器状态和子节点数日志
   - 根据日志判断是可见性问题还是生成问题
   - 修复并测试
2. 测试多关卡流程（第三关、第四关...）
3. 完善词条系统的 TODO
4. 优化游戏体验

---

**重要提醒：**
- 第一关完全正常，问题只出现在第二关
- 代码逻辑应该是对的，可能是状态或属性问题
- 已经添加了调试日志，明天直接看日志就能定位问题
